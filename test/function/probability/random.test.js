"use strict";

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var _assert = require("assert");

var _assert2 = _interopRequireDefault(_assert);

var _index = require("../../../index");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('random', function () {
  // Note: random is a convenience function generated by distribution
  // it is tested in distribution.test.js

  it('should have a function random', function () {
    _assert2.default.equal(_typeof(_index.indexjs.random), 'function');
  });

  it('should LaTeX random', function () {
    var expression = _index.indexjs.parse('random(0,1)');
    _assert2.default.equal(expression.toTex(), '\\mathrm{random}\\left(0,1\\right)');
  });
});
