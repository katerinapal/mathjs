var logjs_exportedObj = {
  'name': 'log',
  'category': 'Arithmetic',
  'syntax': [
    'log(x)',
    'log(x, base)'
  ],
  'description': 'Compute the logarithm of a value. If no base is provided, the natural logarithm of x is calculated. If base if provided, the logarithm is calculated for the specified base. log(x, base) is defined as log(x) / log(base).',
  'examples': [
    'log(3.5)',
    'a = log(2.4)',
    'exp(a)',
    '10 ^ 4',
    'log(10000, 10)',
    'log(10000) / log(10)',
    'b = log(1024, 2)',
    '2 ^ b'
  ],
  'seealso': [
    'exp',
    'log10'
  ]
};

var logjs_exportedObj_name = "log";
var logjs_exportedObj_category = "Arithmetic";
var logjs_exportedObj_syntax = ["log(x)", "log(x, base)"];
var logjs_exportedObj_description = "Compute the logarithm of a value. If no base is provided, the natural logarithm of x is calculated. If base if provided, the logarithm is calculated for the specified base. log(x, base) is defined as log(x) / log(base).";

var logjs_exportedObj_examples = [
  "log(3.5)",
  "a = log(2.4)",
  "exp(a)",
  "10 ^ 4",
  "log(10000, 10)",
  "log(10000) / log(10)",
  "b = log(1024, 2)",
  "2 ^ b"
];

var logjs_exportedObj_seealso = ["exp", "log10"];
export { logjs_exportedObj_name as name, logjs_exportedObj_category as category, logjs_exportedObj_syntax as syntax, logjs_exportedObj_description as description, logjs_exportedObj_examples as examples, logjs_exportedObj_seealso as seealso };